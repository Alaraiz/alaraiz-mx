Eres el ingeniero de implementación senior para el proyecto A La Raíz (Next.js 16 App Router, LibSQL/Turso, en /Users/valedev/Documents/AlaRaiz/alaraiz-mx). El objetivo del usuario: dejar el panel admin como un CMS + CRM + pasarela de pago (arquitectura lista, sin hardcodear qué pasarela) + calendario de actividades + reservas públicas desde la landing, TODO conectado a Turso, sin bugs, premium y seamless. Al final solo deba poner las credenciales de Turso en .env y funcionar 100%.

CONTEXTO YA EXISTENTE (NO lo rompas, úsalo):
- src/lib/db.ts: cliente libsql listo. Lee TURSO_DATABASE_URL / TURSO_AUTH_TOKEN. Fallback a file:local.db.
- src/lib/schema.ts: exporta `migrate()` que crea TODAS las tablas con CREATE TABLE IF NOT EXISTS: users, experiences, facilitators, availability, customers, crm_events, reservations, folders. Ya incluye columnas de pago en reservations (payment_status, payment_method, payment_reference, amount, status). USA este schema tal cual; NO lo reescribas.
- APIs admin existentes (respeta sus rutas y comportamiento): /api/admin/experiences (GET/POST/PUT/DELETE), /api/admin/customers, /api/admin/crm-events, /api/admin/reservations, /api/admin/availability, /api/admin/data (agrega todo), /api/auth/login, /api/auth/logout, /api/auth/password. 
- Managers UI: src/app/admin/experience-manager.tsx, crm-manager.tsx, dashboard.tsx, login/page.tsx, layout.tsx.
- El data route asume tablas: experiences, availability (JOIN experiences), customers, reservations (JOIN customers+experiences), crm_events, folders, facilitators.
- src/scripts/seed.ts existe (revísalo; debe crear un admin inicial si no hay users).

TU TAREA (entrega funcional, sin bugs):

1) MIGRACIÓN AUTOMÁTICA A TURSO
- Crea un script npm `db:migrate` (ej. src/scripts/migrate.ts ejecutable con tsx/node) que importe `migrate` de @/lib/schema y lo corra. Añade el script a package.json ("db:migrate": "tsx src/scripts/migrate.ts" o el runner disponible).
- Haz que `migrate()` se ejecute automáticamente al arrancar el server (un helper `ensureMigrated()` en lib/db.ts que corra migrate() una sola vez por proceso, llamado desde las rutas API o un instrumentation.ts de Next). Esto garantiza que al conectar Turso las tablas existan.
- Verifica que el seed cree un admin (email del .env o uno por defecto documentado en .env.example) para poder entrar al panel.

2) ARQUITECTURA DE PASARELA PLUGGABLE (sin hardcodear cuál)
- Crea src/lib/payments/ con:
  - types.ts: interfaz `PaymentGateway { createCheckout(input): Promise<{url: string; reference: string}>; verifyWebhook(payload, signature): Promise<{reference: string; status: 'paid'|'failed'}> }` y tipos CheckoutInput { amount, currency, description, reservationId, customerEmail }.
  - index.ts: `getGateway()` que lea PAYMENT_PROVIDER del entorno ('stripe' | 'mercadopago' | 'manual') y devuelva el adaptador. Por defecto 'manual'.
  - adapters/manual.ts: adaptador que simula checkout (genera reference, devuelve url a /reserva/confirmacion?ref=... sin cobro real) — para que el flujo funcione end-to-end sin pasarela real.
  - adapters/stripe.ts y adapters/mercadopago.ts: STUBS que tengan la interfaz implementada pero lancen/registren "no configurado" si faltan keys, con TODO claro de qué vars de entorno necesitan (STRIPE_SECRET_KEY, MERCADOPAGO_TOKEN). NO instales SDKs pesados; usa fetch a sus APIs o deja la firma lista para conectar.
- Documenta en .env.example las vars: PAYMENT_PROVIDER, STRIPE_SECRET_KEY, MERCADOPAGO_TOKEN, PAYMENT_WEBHOOK_SECRET.

3) RESERVA PÚBLICA + CHECKOUT (pago inmediato)
- API /api/public/reservations (POST): recibe { experienceId, availabilityId, customer: {name,email,phone}, attendeesCount }. Crea/ubica customer, crea reservation (status 'pending', payment_status 'unpaid', amount = price*attendees), llama getGateway().createCheckout(...). Devuelve { checkoutUrl, reference }.
- Marca availability.booked += attendees (control de capacidad; rechaza si se excede).
- Webhook /api/public/payments/webhook: recibe notificación de la pasarela, verifica firma con PAYMENT_WEBHOOK_SECRET, y si paid: actualiza reservation a status 'confirmed', payment_status 'paid', payment_reference. Para adaptador 'manual', expón también /api/public/reservations/confirm?ref= que marque como pagado (simula el retorno de la pasarela).
- Página pública de reserva en la landing: /reservar/[slug] (o modal en landing) que muestre la experiencia, fechas disponibles (availability), selector de cupos, datos del cliente y botón "Reservar y pagar" → llama /api/public/reservations → redirige a checkoutUrl. Al volver/confirmar, muestra confirmación.
- La confirmación debe verse premium (estado claro, datos de la reserva, referencia).

4) CALENDARIO DE ACTIVIDADES
- Vista admin (/admin, sección calendario o nueva ruta /admin/calendar) que lea availability + reservations y muestre los días con cupos y reservas. Puede ser una tabla/lista agrupada por fecha (no necesitas librería pesada de calendario; un grid simple por semana está bien).
- En la landing pública, sección "Próximas fechas" que liste availability abierto de experiencias publicadas (lee /api/public/availability o reusa /api/admin/data con auth adecuado).

5) PULIDO SIN BUGS + BUILD VERDE
- Revisa experience-manager.tsx, crm-manager.tsx, dashboard.tsx: corrige cualquier bug (manejo de nulls, fetches sin try/catch, estados colgados). No reescribas de cero; mejora lo existente.
- Asegura que el auth (JWT/cookie) proteja rutas admin y que el middleware.ts redirija no-autenticados a /admin/login.
- Tipado estricto: el build de Vercel hace type-check. Corrige errores.
- NO hagas deploy. Haz commit: `git commit -m "feat(admin): CMS+CRM+payments architecture, public reservations, calendar, auto-migrate"`.

VERIFICACIÓN QUE DEBES HACER ANTES DE TERMINAR:
- `npm run db:migrate` crea las tablas en local.db (o Turso si hay creds) sin error.
- `npm run build` pasa (Compiled successfully, sin errores de tipo).
- Smoke test manual (puedes usar node o curl contra `next dev`): login admin, crear experiencia, crear availability, reservar vía /api/public/reservations con provider 'manual', confirmar vía /api/public/reservations/confirm?ref=, verificar que reservation quede 'confirmed'/'paid' y availability.booked aumente. Reporta el resultado del smoke test.
- Reporta: archivos creados/modificados, qué falta conectar (vars de pasarela real), y confirmación de build + smoke test.

NO preguntes; ejecuta con criterio. Si algo del contexto no coincide (ej. rutas distintas), inspecciona el repo y adapta sin romper lo existente.
