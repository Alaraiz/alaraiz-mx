Eres el ingeniero de pulido para el proyecto A La Raíz (Next.js 16 App Router, LibSQL/Turso) en /Users/valedev/Documents/AlaRaiz/alaraiz-mx. El sistema ya funciona end-to-end (login admin, crear experiencias, availability, reserva pública con pasarela 'manual', confirmación, reservation queda 'confirmed'/'paid'). El smoke test pasó. Ahora hay 3 detalles de calidad que deben quedar perfectos porque este proyecto será una PLANTILLA copy-pasteable para otros proyectos. Arrégralos sin romper lo existente.

CONTEXTO:
- lib/db.ts usa @libsql/client con TURSO_DATABASE_URL / TURSO_AUTH_TOKEN (fallback file:local.db).
- Rutas API en src/app/api/**. Los POST de admin devuelven solo { ok: true } sin el id creado.
- La reserva pública (/api/public/reservations) con PAYMENT_PROVIDER=manual crea reservation y devuelve checkoutUrl + reference. El checkoutUrl sale apuntando a localhost:3000 (hardcodeado en algún lado) en vez de la URL real del sitio.
- El reservation confirma bien (status confirmed, paymentStatus paid, amount = price*attendees).

TAREAS (ordén y prioriza):

1) DEVOLVER EL ID EN POSTS
- En src/app/api/admin/experiences/route.ts (POST) y src/app/api/admin/availability/route.ts (POST): después del INSERT, haz un SELECT para obtener el id recién creado (o usa `RETURNING id` / `last_insert_rowid()` según libsql) y devuelve `{ ok: true, id: <el_id> }`. Así el frontend puede encadenar sin re-consultar.
- Aplica lo mismo a cualquier otro POST admin que cree filas y no devuelva id (customers, crm-events, reservations si aplica). Revisa y homologa.

2) CHECKOUTURL CON URL REAL DEL SITIO
- El checkoutUrl generado por el adaptador manual (src/lib/payments/adapters/manual.ts) y cualquier redirect debe usar la URL base real, NO localhost:3000 hardcodeado.
- Usa, en este orden: process.env.NEXT_PUBLIC_SITE_URL, luego la cabecera Host de la request (req.headers.get('host') con protocolo http/https), y solo como último fallback 'http://localhost:3000'.
- Asegúrate de que el adaptador reciba la request (o la baseUrl) para construir la URL correcta. No dejes rutas absolutas hardcodeadas a puertos específicos.
- Verifica también /api/public/reservations/confirm y /reservar/confirmacion usen la misma lógica si generan links.

3) VERIFICAR CALENDARIO ADMIN Y SECCIÓN "PRÓXIMAS FECHAS" EN LANDING
- Revisa src/app/admin/calendar.tsx (o la ruta que lo renderice): debe leer availability + reservations y mostrar slots con capacidad y reservas por fecha sin errores. Si falta o está incompleto, complétalo (lista agrupada por fecha, barra de capacidad booked/capacity, formulario para crear disponibilidad).
- Revisa la sección "Próximas fechas" en la landing (src/app/(landing)/page.tsx o componente): debe consumir /api/public/availability y listar fechas abiertas de experiencias publicadas. Si no está o falla, enlázala correctamente.
- Confirma que ambas vistas manejen el caso de lista vacía (sin datos) sin crashear.

REGLAS:
- TypeScript estricto: el build de Vercel hace type-check. No dejes errores.
- NO reescribas de cero lo que ya funciona; solo los 3 puntos.
- NO cambies el schema de DB ni las tablas.
- NO hagas deploy.
- Al terminar: `npx next build` debe pasar (Compiled successfully). Luego haz un smoke test rápido: crear experiencia vía POST /api/admin/experiences y confirmar que la respuesta incluye `id`; crear availability y confirmar `id`; reservar vía /api/public/reservations y confirmar que checkoutUrl usa la URL real (no localhost:3000 hardcodeado, sino el host de la request o NEXT_PUBLIC_SITE_URL). Reporta resultados del smoke test.
- Commit: `git commit -m "fix(admin): return created ids, real checkout base url, calendar/landing wiring"`.

NO preguntes; ejecuta con criterio. Si algo del contexto no coincide (rutas distintas), inspecciona el repo y adapta sin romper lo existente.
